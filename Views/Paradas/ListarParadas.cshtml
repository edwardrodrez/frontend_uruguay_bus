@model IEnumerable<Models.Entities.Parada>

@{
    ViewData["Title"] = "ListarParadas";
}

<br />
<div class="card" style="background: bottom; border: none; margin: 20px">
    <div class="form-row" style="background: white;margin-top: 100px;">
        <div class="card-body" style="display:flex;align-items:center;justify-content:space-between">
            <h4 class="card-title">Lista de Paradas en la Empresa</h4>
            <a asp-action="Create" class="btn btn-info">Añadir Parada</a>
        </div>
    </div>
</div>
<br />

<div class="table-responsive" style="margin:20px">
    <table class="table table-bordered table-condensed table-striped table-hover sortable" style="width:95%">
        <thead style="background: royalblue; color: white;">
            <tr style="text-align:center">
                <th>
                    @Html.DisplayNameFor(model => model.idParada)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.nombre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.geoReferencia)
                </th>
                <th>
                    Acciones
                </th>
            </tr>
        </thead>
        @if (Model.Count() == 0)
        {
            <tr>
                <td colspan="10">No hay Paradas creadas.</td>
            </tr>
        }
        else
        {
            <tbody>
                @foreach (var item in Model)
                {
                    <tr style="background:white">
                        <td>
                            @Html.DisplayFor(modelItem => item.idParada)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.nombre)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.geoReferencia)
                        </td>
                        <td>
                            <a class="btn btn-success" asp-action="Edit" asp-route-id="@item.idParada"><i class="fa fa-marker fa-lg"></i></a>
                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.idParada"><i class="fa fa-trash-alt fa-lg"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        }
    </table>
</div>